import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('DeviceStats smoke test', (tester) async {
    // Placeholder smoke test; the real UI requires platform channels
    // (usage stats, battery) not available in unit tests.
    expect(true, isTrue);
  });
}