<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application
        android:label="PipStats"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <receiver
            android:name=".UsageReceiver"
            android:exported="false" />
        <!-- Re-arms monitoring after a reboot: without it the service stayed
             down until the user next opened the app, leaving a gap in the
             usage history.

             directBootAware lets the receiver *run* before first unlock, but
             it cannot start monitoring that early: ForegroundService is not
             directBootAware, so until the user unlocks, the system answers the
             start with "Unable to start service ...: not found" (verified on a
             Seeker, 2026-09-25). Collection therefore resumes at
             BOOT_COMPLETED, not at LOCKED_BOOT_COMPLETED. Making it genuinely
             Direct Boot capable would mean marking the service too and moving
             its database into device-protected storage.

             Anything this receiver touches before unlock must live in
             device-protected storage — see MonitoringConsent, which crashed
             the whole process here until it did. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:directBootAware="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>
        <!-- specialUse, not dataSync. Android 15 caps dataSync at six hours a
             day and then *crashes* the app: first
             ForegroundServiceDidNotStopInTimeException when the budget runs
             out, then ForegroundServiceStartNotAllowedException on every
             restart. Continuous device monitoring is exactly what that cap
             exists to stop, so the type was wrong rather than the usage. -->
        <service
            android:name=".ForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Continuous on-device battery and app-usage
                    measurement. The data never leaves the device and no
                    standard foreground service type covers passive telemetry
                    of the device itself." />
        </service>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>
    <!-- Declared by androidx.test:core, which reaches the RELEASE classpath
         through mobile-wallet-adapter-clientlib-ktx:2.2.0-agp9-beta1 ->
         androidx.test.ext:junit-ktx. That beta packages a test dependency as a
         regular one; nothing in the app or in wallet signing reorders a task,
         so the permission is stripped rather than shipped. -->
    <uses-permission android:name="android.permission.REORDER_TASKS"
        tools:node="remove"/>
    <queries>
        <!-- Mobile Wallet Adapter: makes wallet apps visible on Android 11+
             package-visibility rules without relying on QUERY_ALL_PACKAGES. -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <category android:name="android.intent.category.BROWSABLE"/>
            <data android:scheme="solana-wallet"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>